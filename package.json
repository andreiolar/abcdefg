{
  "name": "lookup-service-level-1",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.17.1"
  }
}
